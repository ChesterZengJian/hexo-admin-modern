import { http } from './http';
import { setToken, getToken, removeToken } from './token';
import { history } from './history';
import { getUsername, setUsername, removeUsername } from './user';

export {
    http,
    setToken,
    getToken,
    removeToken,
    getUsername,
    setUsername,
    removeUsername,
    history,
};
